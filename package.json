{
  "builds": [
    { "src": "api/log-ip.js", "use": "@vercel/node" },
    { "src": "package.json", "use": "@vercel/static-build" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1" }
  ],
  "buildCommand": "npm run build",
  "outputDirectory": "build"
}
